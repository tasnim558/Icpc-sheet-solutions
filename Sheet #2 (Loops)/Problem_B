#include <stdio.h>
int main(){
    int N;
    int found = 0;
    scanf("%d", &N);
    for (int i = 2; i <= N; i += 2) {
        printf("%d\n", i);
        found = 1;
    }
    if (found == 0) {
        printf("-1\n");
    }
}
